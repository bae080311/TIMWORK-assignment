- **데이터 분석**: 데이터를 처음 봤을 때 몇 가지 구조적으로 개선하고 싶은 부분이 눈에 띄었습니다.

  첫 번째로, 최상위의 `disciplines` 필드가 `{ name: string }` 하나짜리 객체를 값으로 갖는 Record 형태인데, 속성이 하나뿐이라면 배열로 표현하는 것이 더 직관적일 것 같았습니다.

  두 번째로, 객체의 키 자체가 이미 `id` 역할을 하는데 값 내부에도 `id`를 다시 정의하는 부분은 중복이라고 느꼈습니다.

- **접근 방식**: 먼저 도메인 용어(도면, 공종, 구역, 리비전)를 이해하는 데 시간을 쏟았습니다. 생소한 도메인이라 단어의 의미를 파악한 뒤에야 데이터 구조가 눈에 들어왔습니다. 요구사항을 정리한 다음 metadata.json을 보며 TypeScript 타입을 정의했고, 이 과정에서 데이터의 계층 구조(도면 -> 공종 -> 구역/리비전)를 이해할 수 있었습니다. 타입 정의가 끝나면 selector로 데이터를 정형화하고, 그 이후에 UI를 설계하는 순서로 진행했습니다.

- **UI 설계 결정**:
  - 도면 하나에 공종별 이미지, 리비전 이력, 레이어 오버레이 세 가지 기능이 모두 들어가야 했는데, 이를 한 화면에 전부 나열하면 정보 밀도가 너무 높아진다고 판단했습니다. 그래서 탭으로 분리해 각 기능에 집중할 수 있도록 했습니다.
    - **공종별 탭**: 공종마다 리비전 이미지를 독립적으로 선택해서 볼 수 있습니다. 구역이 있는 공종은 구역 단위로도 따로 확인 가능합니다.
    - **이력 탭**: 전체 리비전을 날짜순으로 펼쳐 보여줍니다. 어떤 공종에서 언제 변경이 있었는지 흐름을 파악하는 용도입니다.
    - **오버레이 탭**: 공종별 이미지를 `mix-blend-multiply`로 겹쳐 어떤 공종이 어느 위치에 작업되는지 한눈에 비교할 수 있습니다.
  - 고려했던 대안으로는 탭 없이 공종을 아코디언으로 펼치는 방식이 있었습니다. 한 화면에서 스크롤로 모든 정보를 볼 수 있다는 장점이 있지만, 도면이 중요하고 뷰를 표현하기 어렵고 화면이 길어져 집중도가 떨어진다고 판단해 탭 방식을 선택했습니다.

- **기술 선택**: 상태 관리는 별도 라이브러리 없이 React의 `useState`만 사용했습니다. 데이터가 정적 JSON 파일 한 개에서 오고 서버 통신이 없기 때문에 전역 상태가 필요한 상황이 없었고, 탭 선택이나 레이어 토글처럼 컴포넌트 단위의 로컬 상태만으로 충분했습니다. 스타일링은 Tailwind CSS를 사용했습니다. 한국어 폰트와 브랜드 컬러 등 커스텀 디자인 토큰을 `@theme`에서 관리할 수 있고, 유틸리티 클래스로 빠르게 반응형 레이아웃을 작성할 수 있었습니다. 아키텍처는 Feature-Sliced Design(FSD)을 따랐습니다. `shared → entities → widgets → pages` 단방향 의존성 규칙 덕분에 어디에 코드를 두어야 하는지 기준이 명확했고, 기능이 명확하게 보였습니다.

- **어려웠던 점 및 개선 방안**: 처음 접하는 도메인이라 용어 이해에 시간이 걸렸고, 데이터 구조가 복잡해 타입을 정의하는 과정이 쉽지 않았습니다. 특히 이미지 정렬 문제가 예상보다 까다로웠습니다. 공종 base 이미지(PNG, 4962×3508)와 리비전 이미지(JPEG, 4928×3456)의 픽셀 크기가 달라 `object-contain`으로 겹쳤을 때 종횡비 차이로 인해 미세한 오프셋이 생겼습니다. 오버레이 이미지에 `object-fill`을 적용해 기준 이미지 영역에 강제로 맞추는 방식으로 해결했는데, 크기 차이가 ~0.8% 수준이라 왜곡은 거의 눈에 띄지 않습니다. metadata에 `imageTransform` 필드(x, y, scale, rotation)가 있어 더 정밀하게 보정할 수 있지만 시간 관계상 완전히 활용하지 못했습니다. 시간이 더 주어진다면 이 값을 CSS transform으로 적용해 이미지를 픽셀 단위로 정확하게 정렬하고 싶습니다.
